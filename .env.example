# Aperture IDP Environment Configuration
# Copy this file to .env.local and fill in your values

# ===================================
# GitHub Integration
# ===================================

# Enable or disable GitHub catalog import feature
# Set to 'true' to enable importing catalog files from GitHub repositories
GITHUB_ENABLED=false

# GitHub Personal Access Token
# Required if GITHUB_ENABLED is true
# 
# How to create a token:
# 1. Go to GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
# 2. Click "Generate new token (classic)"
# 3. Give it a descriptive name (e.g., "Aperture IDP")
# 4. Select the 'repo' scope (Full control of private repositories)
# 5. Click "Generate token" and copy the token
# 
# Note: Keep this token secure and never commit it to version control
GITHUB_TOKEN=your_github_personal_access_token_here

# ===================================
# Next.js Configuration (Optional)
# ===================================

# Uncomment and set these if needed for your deployment

# The URL of your application (used for absolute URLs)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
# NODE_ENV=development

# ===================================
# Future Integrations (Placeholder)
# ===================================

# GitLab Integration (Not yet implemented)
# GITLAB_ENABLED=false
# GITLAB_TOKEN=

# Bitbucket Integration (Not yet implemented)
# BITBUCKET_ENABLED=false
# BITBUCKET_TOKEN=
